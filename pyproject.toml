[project]
name = "bookmarks-checker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiosqlite>=0.22.1",
]

[project.scripts]
start = "start:start"

[tool.uv]
package = true
